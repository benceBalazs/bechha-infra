Login function call with a valid user and password


query for actively running evaluation session “IVADL2024”

submit
- evaluationId
- the task name
- array of ApiClientAnswer
  - text (can be null)
  - the media item (video ID Video name without file extension)
  - the media collection name (“IVADL”), 
  - the start time in milliseconds
  - end time in milliseconds (can be the same as start). 
  
Please note that for the latter data you need to know the frame-rate of the video (it is advisable to store it in the
database). 